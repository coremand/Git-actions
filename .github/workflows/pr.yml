name: pull_request practice


on:
    pull_request:
        branches: ['main']

jobs:

    install:
        runs-on: ubuntu-latest

        steps:
            - uses: actions/setup-node@v4
              with:
                node-version: latest
            - run: npm ci
            - run: node --version